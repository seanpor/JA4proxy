apiVersion: apps/v1
kind: DaemonSet
metadata:
  namespace: {{ .Values.namespace | default "ja4proxy" }}
  name: ja4proxy
  labels:
    app: ja4proxy
  annotations:
    # checkov:skip=CKV_K8S_14: latest tag allowed in POC
    # checkov:skip=CKV_K8S_43: digest not required in POC
    # checkov:skip=CKV_K8S_35: secrets as environment variables allowed
spec:
  selector:
    matchLabels:
      app: ja4proxy
  template:
    metadata:
      labels:
        app: ja4proxy
        metrics: "true"   # Selected by ServiceMonitor
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: "false"
    spec:
      automountServiceAccountToken: false
      priorityClassName: system-node-critical
      terminationGracePeriodSeconds: 30
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      containers:
      - name: ja4proxy
        securityContext:
          {{- toYaml .Values.securityContext | nindent 10 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        - containerPort: 9090
          name: metrics
          protocol: TCP
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        # JA4PROXY-2026-0046: liveness and readiness probes — without these
        # a hung proxy pod continues receiving traffic from the Service
        # load-balancer, and no automatic restart recovers the pod. Both
        # probes target /health on the metrics port (9090) which the Go
        # proxy implements as a cheap, side-effect-free check.
        livenessProbe:
          {{- toYaml .Values.probes.liveness | nindent 10 }}
        readinessProbe:
          {{- toYaml .Values.probes.readiness | nindent 10 }}
        env:
        # The Go proxy reads config/proxy.yml which expands ${REDIS_HOST},
        # ${REDIS_PORT}, ${REDIS_PASSWORD} from env. Set them directly rather
        # than REDIS_URL (which the proxy does not parse).
        {{- if not .Values.redis.external }}
        # Bundled Redis StatefulSet (redis.yaml) — service name is ja4proxy-redis.
        - name: REDIS_HOST
          value: "ja4proxy-redis"
        - name: REDIS_PORT
          value: "6379"
        {{- else }}
        - name: REDIS_HOST
          value: {{ .Values.redis.host | default "redis" | quote }}
        - name: REDIS_PORT
          value: {{ .Values.redis.port | default 6379 | quote }}
        {{- end }}
        - name: REDIS_PASSWORD
          {{- if .Values.secrets.create }}
          valueFrom:
            secretKeyRef:
              name: ja4proxy-secrets
              key: REDIS_PASSWORD
              optional: true
          {{- else }}
          value: {{ .Values.secrets.redisPassword | default "" | quote }}
          {{- end }}
        - name: ABUSEIPDB_API_KEY
          {{- if .Values.secrets.create }}
          valueFrom:
            secretKeyRef:
              name: ja4proxy-secrets
              key: ABUSEIPDB_API_KEY
              optional: true
          {{- end }}
        {{- if .Values.config.enabled }}
        volumeMounts:
        - name: config-volume
          mountPath: {{ .Values.config.mountPath }}
          subPath: {{ .Values.config.subPath }}
        {{- end }}
      {{- if .Values.config.enabled }}
      volumes:
      - name: config-volume
        configMap:
          name: ja4proxy-config
      {{- end }}
