---
# DEPRECATED (phase-802, 2026-07-21): this template moved to
# deploy/integrations/gitlab-ci/ja4proxy-policy.yml, alongside the other
# customer-facing SIEM/SOAR/policy integration templates under deploy/integrations/.
# This is a redirect for pipelines that still `include: local:` the old path —
# it is not maintained here and will be removed in a future release.
#
# Action required: update your .gitlab-ci.yml to include the new path directly:
#   include:
#     - local: 'deploy/integrations/gitlab-ci/ja4proxy-policy.yml'

include:
  - local: 'deploy/integrations/gitlab-ci/ja4proxy-policy.yml'
