- **`ja4proxy-management` base hardening, stage 1: Alpine rebase (Phase 801)**: re-based `deploy/docker/Dockerfile.management` from Debian `python:3.14.0-slim` (41 HIGH/CRITICAL findings, 36 of them no-fix OS packages) to the same digest-pinned `python:3.14.6-alpine3.24` base already used by `analytics`/`tarpit`/`test`/`trafficgen` (Phase 317). All native deps (`cryptography`, `xmlsec`/`python3-saml`, `lxml`, `bcrypt`) resolved via prebuilt musllinux wheels — no source compilation needed. Verified with a full HTTP-level smoke test against the live built image (health check, CSRF token issuance, password login exercising `bcrypt` + JWT signing, an authenticated request, and the SAML metadata route executing cleanly) rather than just "the image builds." Fixed a pre-existing bug found in the process: the Dockerfile's own `HEALTHCHECK` had always pointed at `/health`, which doesn't exist — only `/api/v1/health` does; `docker-compose.poc.yml`'s own healthcheck override was masking this. Image size dropped 357MB → 204MB. Scans **0 HIGH/CRITICAL** (one dated `.trivyignore` residual, `CVE-2024-23342`/ecdsa, upstream won't-fix — tracked for the phase-304 PyJWT migration). `ja4proxy-management` is now gated by `scan-first-party`. See `docs/phases/PHASE_801.md`; the SAML signature-tampering adversarial test (stage 2) is tracked separately, sequenced after Phases 806-808 land a real CI-verified safety net for `management/tests/`.
