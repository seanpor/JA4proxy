- **Security-scan gate CVE fix + third-party image list derivation (Phase 810)**: fixed issue #364 (weekly scheduled `make scan` failure) — a newly-indexed CRITICAL CVE (`GHSA-r277-6w6q-xmqw`, a `kin-openapi` auth-bypass) in `grafana/grafana:13.0.2-ubuntu`, confirmed unfixed even in the latest published tag. Added a dated `.trivyignore` exception and bumped Grafana to `13.0.4-ubuntu` (18 fewer findings, zero new). While fixing the root cause, found that `make scan-images`'s third-party image list (`TRIVY_IMAGES`) was a hand-maintained Makefile variable that had drifted from `docker-compose.monitoring.yml`: one image's tag was stale, and three images (`tecnativa/docker-socket-proxy`, `gcr.io/cadvisor/cadvisor`, `prom/haproxy-exporter`) had been added to the compose file over time and were never added to the scan list, so they'd never been scanned at all. Between them they carried 113 real HIGH/CRITICAL findings. `scripts/check_image_versions.py` now derives that list directly from the compose files (`--list-third-party`, with a hard error on cross-file version drift instead of silently picking one version), so there is exactly one place a third-party image version is ever written down. Bumped `tecnativa/docker-socket-proxy` `0.3.0` → `v0.5.0` (now scans clean) and `gcr.io/cadvisor/cadvisor` `v0.47.2` → `v0.52.1` (57 → 41 findings); added dated `.trivyignore` justifications for what's left, including `prom/haproxy-exporter:v0.15.0` — confirmed abandoned upstream since 2023-02, no newer tag exists (a follow-up phase will evaluate replacing it with HAProxy 2.8's built-in Prometheus exporter). Also reviewed and landed PR #347 (Dependabot's grouped GitHub Actions bump) once `main` carried the CVE fix, after confirming its two breaking changes (`actions/checkout` v7's fork-PR checkout block, `actions/github-script` v9's `require()`/`getOctokit` changes) don't apply to any workflow in this repo. Issue #292 (git history rewrite to purge the `ja4-tap` blob) is explicitly deferred, not silently dropped. See `docs/phases/PHASE_810.md`.
