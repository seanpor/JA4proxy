# Analytics Node dependencies
# Separate from requirements.txt — analytics runs in its own container.
# Keep in sync with versions used by the proxy where packages overlap.

# Redis (sync + async client; aioredis 2.x is a thin shim over redis.asyncio)
redis==8.0.1
aioredis==2.0.1

# HTTP server for /health, /ready, /metrics endpoints
aiohttp>=3.10.11

# Metrics
prometheus-client==0.26.0

# Config + validation
pyyaml==6.0.3
# phase-827: kept in lockstep with requirements.txt, which had to move to
# ~=4.25.1 so semgrep 1.173.0 (needed to clear the click and protobuf CVEs)
# could resolve. The tools image installs all four requirements files into ONE
# environment, so a divergence here is not a difference of opinion between two
# services — it is an unresolvable conflict that fails the image build.
jsonschema~=4.25.1

# Numerical analysis (baseline stats, distribution comparison)
numpy>=1.24

msgpack>=1.2.1  # GHSA-6v7p-g79w-8964 (HIGH) — not a direct dep; local rebuild (Python 3.14,
# matching the image) resolved cleanly with no msgpack at all, so this pin is defensive
# insurance against a transient PyPI resolution picking it up as some package's optional/
# transitive extra, not a confirmed fix for a reproduced local finding.
