# Core dependencies
aiohttp>=3.10.11
uvloop>=0.21.0; sys_platform == "linux" and python_version >= "3.14"  # phase-68
urllib3>=2.7.0  # CVE-2025-50181/66418/66471/CVE-2026-21441 — fixed in 2.5/2.6/2.6.3; 2.7.2 never released
asyncio-throttle==1.0.2
aiodns==4.0.4
cryptography>=50.0.1  # phase-304: was >=45.0.0 — subgroup-attack CVE fixed in 46.0.5
prometheus-client==0.26.0
pyyaml==6.0.3
redis==8.1.0
fastapi>=0.141.1  # Management API + /health/deep endpoint tests
scapy>=2.7.0
sse-starlette>=3.4.10

# Security dependencies
pytricia==1.3.0
geoip2==5.3.0
hypothesis==6.167.1
pip-audit>=2.7.0       # replaces safety (safety 3.x requires auth)
bandit==1.9.4
semgrep>=1.176.0

# Testing dependencies
fakeredis>=2.37.1,<3          # in-process Redis fake for integration tests (P19-G6)
croniter>=6.2.4,<7            # cron expression parser for BackupScheduler (P19-G1)
pytest>=9.1.1          # CVE-2025-71176 fix (fixed in 9.0.3; 9.0.4 never released)
pytest-asyncio>=1.4.0,<2     # 1.x is compatible with pytest 8/9
pytest-cov==7.1.0
pytest-mock==3.15.1
pytest-xdist==3.8.0
pytest-timeout==2.4.0
locust==2.46.4

# Development dependencies
black>=26.5.1
flake8==7.3.0
mypy==2.3.1
isort==9.0.1
pylint==4.0.8

# Compliance and audit
jsonschema==4.25.1
cerberus==1.3.8
# phase-56: Runtime seccomp profile transition (optional — fails open if absent)
# Requires libseccomp-dev on the host: apt-get install libseccomp-dev
# python3-seccomp>=2.5.0

boto3>=1.43.88              # phase-57b: S3 cloud backup adapter (>=1.35 for urllib3 2.x compat)
moto[s3]>=5.2.3            # phase-57b: S3 mock for tests
google-cloud-storage>=3.13.1  # phase-57c: GCS backup adapter (optional). phase-800: floor raised from 2.0 — old api-core pulled protobuf<5 and blew pip resolution depth

tomli>=2.0; python_version < "3.11"  # phase-92: TOML validation fallback for Python 3.10

jsonpath-ng==1.8.0  # phase-85: REST generic TI feed client (src/analytics/ti_feeds/rest_generic.py)

mcp>=1.29.0  # CVE-2025-66416 (requires >=1.28.0) — pinned to match semgrep 1.176+ and force safe resolution

# NOTE (phase-800): two CVEs are acknowledged in the lint gate because semgrep walls them off:
#   - click 8.1.8 (PYSEC-2026-2132): fix is 8.3.3, but semgrep (incl. latest 1.170.0) pins click~=8.1.8.
#   - protobuf 4.25.9 (PYSEC-2026-1805): fix is 5.29.6, but reaching protobuf 5.x needs semgrep>=1.137
#     (OTEL ~=1.37), and every semgrep>=1.137 hard-pins mcp==1.12.2/1.16.0/1.23.3 — which would undo the
#     mcp>=1.28.0 pin above (CVE-2025-66416). Keeping the mcp protection; protobuf stays 4.25.9.
# Remove the two --ignore-vuln flags in the Makefile lint-static target when semgrep ships a release
# with OTEL>=1.37 AND mcp>=1.28 (then pin protobuf>=5.29.6 here).

msgpack>=1.2.2  # GHSA-6v7p-g79w-8964 (HIGH) — transitive dep of locust 2.17.0; pinned to force safe resolution

