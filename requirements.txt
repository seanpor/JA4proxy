# Core dependencies
aiohttp>=3.9,<4
uvloop>=0.21.0; sys_platform == "linux" and python_version >= "3.14"  # phase-68
urllib3>=2.6.3  # CVE-2025-50181/66418/66471/CVE-2026-21441 — upgraded from 1.x
asyncio-throttle==1.0.2
aiodns==4.0.0
cryptography>=44.0.3
prometheus-client==0.25.0
pyyaml==6.0.1
redis==5.0.1
fastapi>=0.104,<1  # Management API + /health/deep endpoint tests
scapy>=2.6.1
sse-starlette>=1.6.5

# Security dependencies
pytricia==1.3.0
geoip2==4.7.0
hypothesis==6.88.1
pip-audit>=2.7.0       # replaces safety (safety 3.x requires auth)
bandit==1.7.5
semgrep>=1.100.0

# Testing dependencies
fakeredis>=2.21,<3          # in-process Redis fake for integration tests (P19-G6)
croniter>=2.0,<3            # cron expression parser for BackupScheduler (P19-G1)
pytest>=9.0.3,<10          # CVE-2025-71176 fix (pre-9.0.3 is vulnerable)
pytest-asyncio>=1.0,<2     # 1.x is compatible with pytest 8/9
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0
pytest-timeout==2.2.0
locust==2.17.0

# Development dependencies
black>=24.3.0
flake8==6.1.0
mypy==1.7.1
isort==5.12.0
pylint==3.0.2

# Compliance and audit
jsonschema==4.20.0
cerberus==1.3.5
# phase-56: Runtime seccomp profile transition (optional — fails open if absent)
# Requires libseccomp-dev on the host: apt-get install libseccomp-dev
# python3-seccomp>=2.5.0

boto3>=1.35              # phase-57b: S3 cloud backup adapter (>=1.35 for urllib3 2.x compat)
moto[s3]>=5.0            # phase-57b: S3 mock for tests
google-cloud-storage>=2.0  # phase-57c: GCS backup adapter (optional)

tomli>=2.0; python_version < "3.11"  # phase-92: TOML validation fallback for Python 3.10

jsonpath-ng==1.6.1  # phase-85: REST generic TI feed client (src/analytics/ti_feeds/rest_generic.py)

mcp>=1.23.0  # CVE-2025-66416 — transitive dep of semgrep; pinned to force safe resolution
